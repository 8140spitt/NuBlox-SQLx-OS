{
  "name": "nublox-sqlx-os-starter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "echo root",
    "clean": "rimraf node_modules && rimraf packages/**/dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.7"
  }
}